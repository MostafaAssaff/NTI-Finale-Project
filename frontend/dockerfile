# Stage 1: Build React app
FROM node:18-alpine as build
WORKDIR /app

COPY package*.json ./
RUN npm ci --silent
COPY . .

ARG REACT_APP_API_URL
ENV REACT_APP_API_URL=${REACT_APP_API_URL}  # Optional: used for scripts

RUN REACT_APP_API_URL=${REACT_APP_API_URL} npm run build
